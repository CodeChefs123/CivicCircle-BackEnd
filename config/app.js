/**
 * This file contains the configuration for the application.
 */

// The port number to listen on. It uses the value from the environment variable PORT if available, otherwise defaults to 3000.
const PORT = process.env.PORT || 3000;

export default PORT;
